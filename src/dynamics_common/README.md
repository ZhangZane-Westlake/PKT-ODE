# Shared trajectory identifiers

This small utility module canonicalizes Open TG-GATEs doses and builds stable treatment identifiers shared by gene selection and downstream dynamics code. It contains no model implementation and writes no artifacts.

